import { DateLocalizedPipe } from './date-localized.pipe';

describe('DateLocalizedPipe', () => {
  it('create an instance', () => {
    const pipe = new DateLocalizedPipe();
    expect(pipe).toBeTruthy();
  });
});
